## Supabase (required)
# Get these from your Supabase project: Project Settings → API
VITE_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

## Cloudinary (required for image uploads in Host Dashboard)
# Cloudinary dashboard → Settings → Upload → Upload presets
# IMPORTANT: This must be an UNSIGNED upload preset (client-side uploads).
VITE_CLOUDINARY_CLOUD_NAME="YOUR_CLOUD_NAME"
VITE_CLOUDINARY_UPLOAD_PRESET="YOUR_UNSIGNED_UPLOAD_PRESET"

## Server-side (Vercel Functions)
# Used by the AI Trip Advisor (/api/ai-trip-advisor). Set these ONLY in Vercel env vars (not in client).
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"
SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"
