# iOS App (SwiftUI)

SwiftUI implementation files are ready in `Merry360xMobile/`.

To run, create an iOS SwiftUI app target in Xcode under this folder and include the provided files.

See `../docs/LAUNCHING_STEP.md`.
